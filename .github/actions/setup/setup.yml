name: Setup and Install

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v4
      with:
        version: 10

    - uses: actions/setup-node@v4
      with:
        version: '20'
        cache: 'pnpm'

    - shell: bash
      run: pnpm install
